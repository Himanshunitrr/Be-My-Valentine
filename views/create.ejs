<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Your Valentine Date</title>
  <style>
    /* Base reset and styling for a clean look */
    body {
      font-family: 'Arial', sans-serif;
      background: #f2f2f2;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }
    /* Container to center and nicely format the form */
    .form-container {
      background: #fff;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      max-width: 400px;
      width: 100%;
    }
    .form-container h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }
    /* Label styling */
    .form-container label {
      display: block;
      margin-top: 15px;
      margin-bottom: 5px;
      color: #555;
    }
    /* Input styling: all inputs take full width and have consistent padding */
    .form-container input {
      width: 100%;
      padding: 10px;
      font-size: 1em;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    /* Submit button styling: same width and padding as the input fields */
    .form-container button {
      margin-top: 25px;
      width: 105%;
      padding: 10px; /* same padding as inputs */
      background: #ff4d4d;
      color: #fff;
      border: 1px solid #ff4d4d;
      font-size: 1em;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .form-container button:hover {
      background: #e04343;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Create Your Valentine Date</h2>
    <form action="/create" method="POST">
      <label for="date">Date:</label>
      <!-- Using input type "date" for a calendar picker -->
      <input type="date" id="date" name="date" required />

      <label for="time">Time:</label>
      <!-- Using input type "time" for a time picker -->
      <input type="time" id="time" name="time" required />

      <label for="place">Place:</label>
      <input type="text" id="place" name="place" placeholder="e.g. The Moonlight Garden" required />

      <label for="activity">Activity:</label>
      <input type="text" id="activity" name="activity" placeholder="e.g. A romantic dinner under the stars" required />

      <button type="submit">Submit</button>
    </form>
  </div>
</body>
</html>
